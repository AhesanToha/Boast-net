import React from 'react';
import Loading from '../../Shared/Loading/Loading';

const Blogs = () => {
    return (
        <div>
            this is Blogs
            <Loading></Loading>
        </div>
    );
};

export default Blogs;